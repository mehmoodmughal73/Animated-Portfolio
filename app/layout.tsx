import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Mehmood Ashraf - DevOps Engineer & Linux Admin',
  description: 'Professional DevOps Engineer and Linux System Administrator specializing in cloud infrastructure, automation, and scalable solutions.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
